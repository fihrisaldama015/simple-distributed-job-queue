type Mutation {
  # Create New Job.
  #
  # idempotencyKey is optional. Passing the same key twice returns the job created by
  # the first call instead of enqueueing a second one.
  Enqueue(task: String!, idempotencyKey: String): Job!
}
